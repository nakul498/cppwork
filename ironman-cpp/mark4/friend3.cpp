#include<bits/stdc++.h>
using namespace std;
class citizen{
    int a;
    public:
    int b;
    priavte:
    int c;
    protected:
    int d;
}
class kid:public citizen{};
int main(){
    return 0
}
for(int i: arr){
     
}
/* here a is private as it access modifier is not specified 
b is public so it could be used by kid class as it is inheriting the citizen class
and also could be used in main function so there are property of public
that it can be use same class, sub classes and also in main function whereas
c can only be used in same class and d can be accessed in same class as well as subclasses
